import React from 'react';
import ReactLoading from 'react-loading';

const Loader = () => (
    <ReactLoading type={'spinningBubbles'} className='loader'  color={'white'}/>
);

export default Loader;
